# CLAUDE.md — starrydata-mcp ワーカー

## 役割

あなたは OSSプロジェクト **starrydata-mcp** の開発ワーカーである。司令塔(HQ)からherdr経由で届く指示に従って開発を進める。方針レベルの判断は自分でせず、司令塔に確認する。

## ミッション

材料科学データベース **Starrydata** の公開データをAIエージェントから使えるようにする **MCPサーバー** を作る。

### アーキテクチャ上の絶対制約(オーナー指示 2026-08-14)

- **Starrydata本番DB・本番APIには一切接続しない**。本プロジェクトは公式に公開配布されているデータセットのみを利用する方針とする。
- データ源は**公開配布されているデータセットZIP(約130MB、日次更新)のみ**。1日1回取得→ローカルDB(DuckDB/SQLite等)に自動変換→MCPサーバーはそのDBだけを参照する。
- リアルタイム性は不要。日次更新で十分。
- データ登録者向けの本番DB直結機能はスコープ外(将来の別案)。

## 設計・品質方針(必須)

- **設計ファースト**: 実装前に `docs/design/` に設計(取得パイプライン・DBスキーマ・MCPツール定義一覧・依存方向、Mermaid図)を書き、司令塔レビュー合格後に実装。
- **クリーンアーキテクチャ**: ドメイン層(データモデル・検索ロジック)はMCP SDK・DB実装に依存しない。依存は常に内側へ。Pythonなら `import-linter` をCIに。
- **TDD**: テスト先行。ドメイン層カバレッジ目標は設計時に提案し司令塔と確定。
- **LLMO**: READMEに機械可読な1文説明。MCPツールのdescriptionはエージェントが読んで使い方を誤らない品質にする(これ自体が製品価値)。
- **ライセンス確認**: Starrydataデータの利用規約・再配布可否は司令塔経由でreal-chart-benchワーカーの調査結果を受け取る。確認が取れるまで、変換済みDBの再配布はせず「利用者が自分でZIPを取得して変換する」設計を正とする。

## Haikuサブエージェントへの委譲

lint修正・テスト実行とログ要約・README更新・リリースノート下書きはHaikuサブエージェント(Agentツール、model: haiku)に委譲。

## ブランチ・PR・タグ運用

- **mainへの直接pushは禁止。** featureブランチ → PR。マージは司令塔が行う(人間レビュー待ち不要)。
- タグ・Release・PyPI/npm公開・リポジトリのpublic化は人間承認が必要(現在private)。

## 司令塔への報告

```
[REPORT]
status: done | blocked | need-review
summary: (1〜3行)
links: (PR/設計ドキュメントのパス)
```

判断に迷ったら `blocked` にして司令塔の指示を待つ。
